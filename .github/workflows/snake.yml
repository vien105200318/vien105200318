name: 🐍 Generate Snake Animation

on:
  schedule:
    # Chạy tự động sau mỗi 12 giờ
    - cron: "0 */12 * * *"
  workflow_dispatch:
    # Cho phép chạy thủ công

# CẤP QUYỀN GHI (WRITE) CHO ACTION:
# Rất cần thiết để bot có thể tạo và đẩy nội dung lên branch 'output'
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      # 1. Tạo thư mục 'dist' (Đảm bảo nó tồn tại)
      - name: Create dist directory
        run: mkdir dist
        
      # 2. Tạo Snake SVG (Đã dùng tham số mới 'svg_out_path')
      - name: Generate the snake animation
        uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: vien105200318
          # Đường dẫn đầu ra cho file SVG
          svg_out_path: dist/github-contribution-grid-snake.svg
          gif_out_path: dist/github-contribution-grid-snake.gif
          
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # 3. Triển khai lên branch 'output' (Đã sửa lỗi quyền)
      - name: Deploy to output branch
        uses: crazy-max/ghaction-github-pages@v2
        with:
          target_branch: output
          build_dir: dist # Trỏ đến thư mục chứa file SVG đã tạo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
